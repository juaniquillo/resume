# TODO List

- Refactor Enums: Use labels as values eliminate label method. 
- Check after validation if the "other" value is set then use the "other_*" to insert in the DB.
- Add a schedule job to delete old exports and imports. Add column to inform when it's marked for deletion.