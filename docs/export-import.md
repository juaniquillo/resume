# Exporting & Importing

## JSON Resume Standard
Resume Manager supports import and export using the standard JSON Resume format.

- **Import**: Upload any valid JSON Resume file via the import dashboard. A background job (`ProcessResumeImport`) parses and populates your career sections asynchronously.
- **Export**: Generate structured JSON exports or styled PDF resumes with theme selection and download permissions.
